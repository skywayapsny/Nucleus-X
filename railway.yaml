version: 2
build:
  builder: DOCKERFILE
  dockerfilePath: ./Dockerfile
deploy:
  healthcheckPath: /healthz
  healthcheckTimeout: 60
  restartPolicyType: ON_FAILURE
  restartPolicyMaxRetries: 10
  numReplicas: 1
  startCommand: uvicorn adapter.main:app --host 0.0.0.0 --port 8000
